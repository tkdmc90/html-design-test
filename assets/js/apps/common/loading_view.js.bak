define(["app", "tpl!apps/common/templates/loading.html"],
    function (BrewOptix, loading) {
        BrewOptix.module("Common.Views", function (Views, BrewOptix, Backbone, Marionette, $, _) {

            Views.Loading = Marionette.ItemView.extend({
                template: loading
            });
        });

        return BrewOptix.Common.Views;
    }
);